# =============================================================================
# Nomedia Self-Hosted Configuration
# =============================================================================
# IMPORTANT: Copy this file to ".env" before starting the application
#
# Instructions:
#   1. Copy this file: cp .env.selfhosted .env
#   2. Generate secure secrets (see instructions below)
#   3. Update passwords to secure values
#   4. Run: docker-compose -f docker-compose.selfhosted.yml up -d
# =============================================================================

# =============================================================================
# Application Settings
# =============================================================================
NODE_ENV=production
PORT=8000

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL database settings
DB_NAME=nomedia
DB_USER=nomedia_user
DB_PASSWORD=nomedia_secure_password_change_me
DB_PORT=5432

# =============================================================================
# Security Settings (IMPORTANT - CHANGE THESE!)
# =============================================================================
# Generate random secrets with this command:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Or on Linux/Mac:
#   openssl rand -hex 32

# JWT Secret (for authentication tokens)
JWT_SECRET=CHANGE_ME_RUN_node_-e_console.log_require_crypto_randomBytes_32_toString_hex

# Session Secret (for session management)
SESSION_SECRET=CHANGE_ME_RUN_node_-e_console.log_require_crypto_randomBytes_32_toString_hex

# JWT token expiration (7 days by default)
JWT_EXPIRES_IN=7d

# Password hashing rounds (higher = more secure but slower)
BCRYPT_ROUNDS=12

# =============================================================================
# File Upload Settings
# =============================================================================
# Maximum file upload size in bytes (10MB default)
MAX_FILE_SIZE=10485760

# Upload directory (inside container)
UPLOAD_DIR=/app/uploads

# =============================================================================
# Email Configuration (Optional - for notifications)
# =============================================================================
# Leave empty if you don't want email notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Email examples:
# Gmail: smtp.gmail.com:587 (use app password)
# Outlook: smtp.office365.com:587
# SendGrid: smtp.sendgrid.net:587

# =============================================================================
# Application URLs
# =============================================================================
# These are used for CORS and redirect URLs
API_BASE_URL=http://localhost:8000/api
FRONTEND_URL=http://localhost:8000
CORS_ORIGIN=http://localhost:8000

# If accessing from network, change to your computer's IP:
# API_BASE_URL=http://192.168.1.100:8000/api
# FRONTEND_URL=http://192.168.1.100:8000
# CORS_ORIGIN=http://192.168.1.100:8000

# =============================================================================
# Logging & Debugging
# =============================================================================
LOG_LEVEL=info
DEBUG=false

# For development/troubleshooting, use:
# LOG_LEVEL=debug
# DEBUG=true

# =============================================================================
# QUICK SETUP GUIDE
# =============================================================================
#
# 1. GENERATE SECRETS:
#    Windows (PowerShell):
#      node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
#    Linux/Mac:
#      openssl rand -hex 32
#
#    Copy output to JWT_SECRET and SESSION_SECRET above
#
# 2. CHANGE DATABASE PASSWORD:
#    Replace "nomedia_secure_password_change_me" with a strong password
#
# 3. SAVE THIS FILE AS ".env":
#    Windows: copy .env.selfhosted .env
#    Linux/Mac: cp .env.selfhosted .env
#
# 4. START THE APPLICATION:
#    Windows: start.bat
#    Linux/Mac: ./start.sh
#
# 5. ACCESS:
#    Open browser: http://localhost:8000
#    Default login: admin@nomedia.com / admin123
#
# =============================================================================
