# ====================================
# Nomedia Production Environment File
# ====================================
# IMPORTANT: Copy this to .env.production and update all values!
# Generate secrets: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# ============
# Environment
# ============
NODE_ENV=production
PORT=8000

# ============
# Database
# ============

# Option A: PostgreSQL (RECOMMENDED for production)
DATABASE_TYPE=postgresql
DATABASE_URL="postgresql://nomedia_user:YOUR_PASSWORD@host:5432/nomedia_production"

# Option B: SQLite (Only for small deployments <10 users)
# DATABASE_TYPE=sqlite
# SQLITE_PATH=./nomedia.db

# ============
# Security (CHANGE ALL OF THESE!)
# ============
JWT_SECRET="CHANGE-ME-RUN-node-e-console.log-require-crypto-.randomBytes-32-.toString-hex"
JWT_EXPIRES_IN=7d
SESSION_SECRET="ANOTHER-RANDOM-STRING-DIFFERENT-FROM-JWT-SECRET"
BCRYPT_ROUNDS=12

# ============
# API URLs
# ============
API_BASE_URL=https://your-domain.com/api
FRONTEND_URL=https://your-domain.com

# ============
# CORS
# ============
CORS_ORIGIN=https://your-domain.com

# ============
# Email (Optional - for notifications)
# ============
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# ============
# File Upload
# ============
MAX_FILE_SIZE=10485760
UPLOAD_DIR=uploads

# ============
# Logging
# ============
LOG_LEVEL=info
DEBUG=false

# ============
# Quick Setup Guide
# ============
#
# 1. COPY THIS FILE:
#    cp .env.production.example .env.production
#
# 2. GENERATE JWT_SECRET:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#    Paste result in JWT_SECRET above
#
# 3. GENERATE SESSION_SECRET:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#    Paste result in SESSION_SECRET above
#
# 4. SETUP DATABASE:
#    - Railway: Add PostgreSQL, copy DATABASE_URL from Variables
#    - Render: Create PostgreSQL, copy Internal Database URL
#    - Supabase: Get connection string from Settings → Database
#
# 5. UPDATE URLS:
#    - Replace your-domain.com with your actual domain
#    - Use https:// in production!
#
# 6. TEST:
#    npm start
#
# 7. DEPLOY:
#    Push to GitHub → Railway/Render auto-deploys!
